// export const structuredInvestors = (investors) => investors.map(investor => ({
//     owner: investor.name,
//     refunded: investor.refunded,
//     timestamp: new Date(investor.timestamp.toNumber() * 1000).toJSON(),
//     donation: parseInt(investor.contri)
// }))
